# macOS Finder metadata (this is the stray file that kept showing up)
.DS_Store

# Windows / other OS clutter
Thumbs.db

# Common editor scratch files
*.swp

# VS Code — ignore personal editor state but keep the shared project config
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
